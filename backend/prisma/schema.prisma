datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Participant {
  id                String              @id
  name              String
  email             String
  Status            ParticipantStatus   @relation(fields: [statusId], references: [id])
  statusId          String
  Pair              Pair?               @relation(fields: [pairId], references: [id], onDelete: Cascade)
  pairId            String?
  participantOnTask ParticipantOnTask[]
}

model ParticipantStatus {
  id          String        @id
  name        String
  Participant Participant[]
}

model Pair {
  id           String        @id
  name         String
  participants Participant[]
  Team         Team          @relation(fields: [teamId], references: [id], onDelete: Cascade)
  teamId       String
}

model Team {
  id    String @id
  name  String
  pairs Pair[]
}

model Task {
  id               String              @id
  title            String
  content          String
  ParitipantOnTask ParticipantOnTask[]
}

model ParticipantOnTask {
  id            String      @id
  Status        TaskStatus  @relation(fields: [statusId], references: [id])
  statusId      String
  task          Task        @relation(fields: [taskId], references: [id], onDelete: Cascade)
  taskId        String
  participant   Participant @relation(fields: [participantId], references: [id])
  participantId String
}

model TaskStatus {
  id                String              @id
  name              String
  ParticipantOnTask ParticipantOnTask[]
}

model RemovedParticipant {
  id       String                   @id
  name     String
  email    String
  Status   RemovedParticipantStatus @relation(fields: [statusId], references: [id])
  statusId String
}

model RemovedParticipantStatus {
  id                 String               @id
  name               String
  RemovedParticipant RemovedParticipant[]
}
